import 'package:flutter/material.dart';

class AnimationSwitchExample extends StatelessWidget {
  const AnimationSwitchExample({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('动画切换'),
    );
  }
}
