import 'package:flutter/material.dart';

class AnimationDecoratedExample extends StatelessWidget {
  const AnimationDecoratedExample({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('过渡'),
    );
  }
}
